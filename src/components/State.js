//JS object wherre property values of the component are StereoPannerNode
//Managed inside of the component instead of being passed into it
//(like props)

import { useState }  from 'react';

const State = () => {
    
    return (
        <>
        <button>-</button>
        <span> 0 </span>
        <button>+</button>
        </>
    )
}

function withdraw()

export default State;